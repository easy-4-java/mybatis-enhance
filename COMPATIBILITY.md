# 版本兼容策略

`mybatis-enhance` 的主版本与 Java 基线绑定，避免在同一版本线中混用不同字节码、语言特性和三方组件基线。

| 版本线 | Java 基线 | 依赖与代码约束 | 当前状态 |
| --- | --- | --- | --- |
| `1.0.x` | JDK 8 | 依赖必须支持 Java 8；Java 源码不得使用 Java 9+ 语法；JSqlParser 使用 3.1 兼容实现 | 当前维护分支 |
| `2.0.x` | JDK 17 | 可升级到支持 Java 17 的三方组件，并使用 Java 17 语言特性 | 同步时单独验证 |
| `3.0.x` | JDK 21 | 可升级到支持 Java 21 的三方组件，并使用 Java 21 语言特性 | 同步时单独验证 |

跨版本线同步以能力和公共契约一比一为目标，但依赖版本、Maven 编译配置、JSqlParser API 适配和 Java 语法必须分别符合对应 JDK 基线，不能机械复制构建配置。

