<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="io.ddd4j.enhance.integration.CryptoMapper">

    <insert id="insert" useGeneratedKeys="true" keyProperty="id"
            parameterType="io.ddd4j.enhance.integration.CryptoSignatureIntegrationTest$CryptoRecord">
        INSERT INTO crypto_record (mobile, email, signature_value)
        VALUES (#{mobile}, #{email}, #{signatureValue})
    </insert>

    <select id="selectById" resultType="io.ddd4j.enhance.integration.CryptoSignatureIntegrationTest$CryptoRecord">
        SELECT id, mobile, email, signature_value AS signatureValue
        FROM crypto_record
        WHERE id = #{id}
    </select>

    <!-- selectIgnoreDecryptById 签名: @IgnoreEncrypted 在接口层声明，MyBatis 照常映射 -->
    <select id="selectIgnoreDecryptById" resultType="io.ddd4j.enhance.integration.CryptoSignatureIntegrationTest$CryptoRecord">
        SELECT id, mobile, email, signature_value AS signatureValue
        FROM crypto_record
        WHERE id = #{id}
    </select>

    <select id="selectList" resultType="io.ddd4j.enhance.integration.CryptoSignatureIntegrationTest$CryptoRecord">
        SELECT id, mobile, email, signature_value AS signatureValue
        FROM crypto_record
    </select>

    <select id="selectIgnoreDecryptBatchIds" resultType="io.ddd4j.enhance.integration.CryptoSignatureIntegrationTest$CryptoRecord">
        SELECT id, mobile, email, signature_value AS signatureValue
        FROM crypto_record
        WHERE id IN
        <foreach collection="list" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
    </select>

    <select id="selectBatchIds" resultType="io.ddd4j.enhance.integration.CryptoSignatureIntegrationTest$CryptoRecord">
        SELECT id, mobile, email, signature_value AS signatureValue
        FROM crypto_record
        WHERE id IN
        <foreach collection="list" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
    </select>

    <update id="updateById" parameterType="io.ddd4j.enhance.integration.CryptoSignatureIntegrationTest$CryptoRecord">
        UPDATE crypto_record
        SET mobile = #{mobile}, email = #{email}, signature_value = #{signatureValue}
        WHERE id = #{id}
    </update>

</mapper>
